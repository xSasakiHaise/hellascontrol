pluginManagement {
    repositories {
        // Wichtig: NeoGradle Plugin kommt von hier
        maven { url = 'https://maven.neoforged.net/releases' }

        // Standard-Quellen
        gradlePluginPortal()
        mavenCentral()
        mavenLocal()
    }
}

rootProject.name = "hellascontrol"
