# LicenseCache

`LicenseCache` (`src/main/java/com/xsasakihaise/hellascontrol/license/LicenseCache.java`) is an immutable snapshot of license state.

## Responsibilities
- Stores validity, entitlement list, and descriptive metadata.
- Builds cache entries from JSON files or remote responses.
