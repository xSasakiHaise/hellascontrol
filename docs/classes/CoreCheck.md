# CoreCheck

`CoreCheck` (`src/main/java/com/xsasakihaise/hellascontrol/api/CoreCheck.java`) provides shared sidemod guard helpers.

## Responsibilities
- Verifies the core mod is present on startup.
- Requires a named entitlement on dedicated servers.
