# HellasControlDebugConfig

`HellasControlDebugConfig` (`src/main/java/com/xsasakihaise/hellascontrol/config/HellasControlDebugConfig.java`) loads debug flags for handshake diagnostics.

## Responsibilities
- Reads `debug.json` from the HellasControl config directory.
- Exposes handshake and packet registry debug flags.
